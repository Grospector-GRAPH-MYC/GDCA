on:
  schedule:
    # Run at 00:00 UTC every day
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  run-backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true

      - name: Install dependencies
        run: poetry install --no-interaction --no-root

      - name: Download Data (Incremental)
        id: download
        run: |
          # Capture output to check for update
          OUTPUT=$(poetry run python scripts/download_data.py --incremental)
          echo "$OUTPUT"
          
          if echo "$OUTPUT" | grep -q "DATA_UPDATED"; then
            echo "updated=true" >> $GITHUB_OUTPUT
          else
            echo "updated=false" >> $GITHUB_OUTPUT
          fi

      - name: Run Backtest
        if: steps.download.outputs.updated == 'true'
        run: |
          poetry run python scripts/run_backtest.py

      - name: Commit and Push Changes
        if: steps.download.outputs.updated == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Add Catalog Changes
          git add catalog/
          
          # Add Results
          git add dca_result.html gdca_result.html comparison_result.html
          
          git commit -m "Auto-update: Backtest results for $(date +'%Y-%m-%d')"
          git push
